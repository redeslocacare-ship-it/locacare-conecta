Baseado na análise dos arquivos `cre.txt`, `.env` e `supabase/config.toml`, identifiquei a seguinte divergência:

1. **Credenciais Corretas (cre.txt)**:

   * Projeto ID: `wwltjlnlutnuypmkwbuy`

   * URL: `https://wwltjlnlutnuypmkwbuy.supabase.co`

   * Token Anon: (Confere com `.env`)

   * Token GitHub: `ghp_...`

2. **Configuração Frontend (.env)**:

   * URL: `https://wwltjlnlutnuypmkwbuy.supabase.co` (Correto)

   * Anon Key: (Correto)

3. **Configuração Supabase CLI (supabase/config.toml)**:

   * Project ID: `hxqxrprywdpybogowiar` (**INCORRETO**)

   * *Impacto*: O sincronizador pode estar tentando acessar o projeto antigo ao rodar comandos da CLI que dependem deste arquivo.

**Plano de Ação:**

1. **Alinhar Configurações**:

   * Atualizar `supabase/config.toml` para usar o Project ID `wwltjlnlutnuypmkwbuy`.

   * Atualizar `scripts/sync_full.py` para ler o ID do projeto dinamicamente do `.env` ou `config.toml`, evitando hardcoding e garantindo consistência.

2. **Validador de Credenciais (`scripts/validate_creds.py`)**:

   * Criar um script dedicado que:

     * Lê `cre.txt` e `.env`.

     * Testa conexão com Supabase (REST probe).

     * Testa conexão com GitHub (API probe).

     * Verifica consistência entre os arquivos.

     * Relata claramente qualquer erro ou divergência.

3. **Verificação Final**:

   * Executar o validador e confirmar que tudo está verde.

