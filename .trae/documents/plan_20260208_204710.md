O erro relatado `A <Select.Item /> must have a value prop that is not an empty string` ocorre porque o componente `Select` do Radix UI (usado internamente pelo shadcn/ui) não aceita `value=""` em `<SelectItem>`. Para representar "Todos" ou uma opção vazia, o valor deve ser algo diferente de string vazia (ex: "todos") ou a lógica deve ser ajustada.

**Plano de Correção:**

1.  **LocacoesPage.tsx**:
    *   Alterar o filtro de status: Mudar o valor de "Todos" de `""` para `"all"` ou similar.
    *   Ajustar a lógica do `Select` para tratar esse valor especial ao filtrar.
    *   No formulário de criação, garantir que os selects (Cliente, Plano, Poltrona) não passem strings vazias para o `SelectItem` se não houver seleção (usar `value={field.value || undefined}` ou similar e garantir que o Item tenha valor válido).

2.  **ClientesPage.tsx**:
    *   Verificar se há Selects com problema similar. (Aparentemente não há Selects nesta página, apenas Inputs, mas revisarei).

3.  **PoltronasPage.tsx**:
    *   Revisar o Select de Status. Garantir que todas as opções tenham valores não vazios.

4.  **PlanosPage.tsx**:
    *   Revisar Selects se houver.

**Ação Imediata**: Vou corrigir o `LocacoesPage.tsx` onde o erro foi explicitamente capturado no console, ajustando o filtro de status e os selects do formulário.

Gostaria que eu aplicasse essa correção técnica para eliminar o erro de console e restaurar a funcionalidade da página?